{
  "name": "rmkaav-foods-site",
  "private": true,
  "description": "Static site for RMKAAV Foods. No build step — assets/ is served as-is.",
  "scripts": {
    "check": "python3 scripts/check_site.py",
    "test": "node scripts/test_functionality.js && node scripts/test_cart.js && node scripts/test_instamojo_helpers.js",
    "verify": "npm run check && npm run test"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
